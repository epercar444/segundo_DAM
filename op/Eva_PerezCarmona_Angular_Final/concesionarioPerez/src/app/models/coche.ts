export class Coche {
  constructor(
    public id: number,
    public marca: string,
    public modelo: string,
    public precio: number,
    public disponible: boolean,
  ) {} //definición atributos coche
}